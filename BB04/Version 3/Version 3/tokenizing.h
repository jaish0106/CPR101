// This is an include guard to prevent multiple inclusions of the header file.
#ifndef TOKENIZING_H
#define TOKENIZING_H

// Include standard libraries needed for the function.
#include <stdio.h>
#include <string.h>

// Function declaration for tokenizing.
// The function returns no value (void) and takes no parameters (void).
void tokenizing(void);

// End the include guard to complete the header file.
#endif